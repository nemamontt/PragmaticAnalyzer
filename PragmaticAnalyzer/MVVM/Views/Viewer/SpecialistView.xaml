<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Viewer.SpecialistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PragmaticAnalyzer.MVVM.Views"
             xmlns:cc="clr-namespace:PragmaticAnalyzer.CastomControls"
             xmlns:assist="clr-namespace:PragmaticAnalyzer.Assistant"
             xmlns:vmViewer="clr-namespace:PragmaticAnalyzer.MVVM.ViewModel.Viewer"
             d:DataContext="{d:DesignInstance Type=vmViewer:SpecialistViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="800">
    <UserControl.Resources>
        <assist:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <TextBlock Text="Наименование ОВУ:"
                   Style="{StaticResource MainTextBlockStyle}" />

        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 Style="{StaticResource RoundedListBoxStyle}"
                 ItemContainerStyle="{StaticResource RoundedListBoxItemStyle}"
                 ItemsSource="{Binding Specialists}"
                 SelectedItem="{Binding SelectedSpecialist}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding NameOrgan}"
                               Style="{StaticResource MainTextBlockStyle}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="0"
                Grid.Column="1"
                Grid.RowSpan="2"
                Margin="5"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="6">
            <WrapPanel  Visibility="{Binding SelectedSpecialist, Converter={StaticResource NullToVisibilityConverter}}">
                <cc:LabeledValueBox LabelText="Наименование вышестоящего ОВУ"
                                    ValueText="{Binding  SelectedSpecialist.NameHighestOrgan}" />

                <cc:LabeledValueBox LabelText="Наименование подчиненного ОВУ"
                                    ValueText="{Binding  SelectedSpecialist.NameSubordinateOrgan}" />

                <cc:LabeledValueBox LabelText="Статус по выявленной уязвимости"
                                    ValueText="{Binding  SelectedSpecialist.StatusVulnerability}" />

                <cc:LabeledValueBox LabelText="Предпринятые действия"
                                    ValueText="{Binding  SelectedSpecialist.ActionsTaken}" />

                <cc:LabeledValueBox LabelText="Список принятых мер защиты"
                                    ValueText="{Binding  SelectedSpecialist.UsingMeasuresToString}" />

                <cc:LabeledValueBox LabelText="Наименования взаимодействующих ОВУ"
                                    ValueText="{Binding  SelectedSpecialist.NameInteractingOrgansToString}" />
            </WrapPanel>
        </Border>

        <StackPanel Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Style="{StaticResource  OutlinedGrayButtonStyle}"
                    Content="Добавить"
                    Command="{Binding AddCommand}" />

            <Button Style="{StaticResource  OutlinedGrayButtonStyle}"
                    Content="Удалить"
                    IsEnabled="{Binding}"
                    Command="{Binding DeleteCommand}" />

            <Button Style="{StaticResource  OutlinedGrayButtonStyle}"
                    Content="Изменить"
                    IsEnabled="{Binding}"
                    Command="{Binding ChangeCommand}" />
        </StackPanel>
    </Grid>
</UserControl>