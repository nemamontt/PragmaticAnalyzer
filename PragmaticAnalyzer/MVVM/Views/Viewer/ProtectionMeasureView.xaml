<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Viewer.ProtectionMeasureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PragmaticAnalyzer.MVVM.Views"
             xmlns:cc="clr-namespace:PragmaticAnalyzer.CastomControls"
             xmlns:assist="clr-namespace:PragmaticAnalyzer.Assistant"
             xmlns:vmViewer="clr-namespace:PragmaticAnalyzer.MVVM.ViewModel.Viewer"
             d:DataContext="{d:DesignInstance vmViewer:ProtectionMeasureViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="800">
    <UserControl.Resources>
        <assist:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Условное обозначение:"
                   Style="{StaticResource MainTextBlockStyle}" />

        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 ItemsSource="{Binding ProtectionMeasures}"
                 SelectedItem="{Binding SelectedProtectionMeasure}"
                 Style="{StaticResource RoundedListBoxStyle}"
                 ItemContainerStyle="{StaticResource RoundedListBoxItemStyle}"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DispayedName}"
                               Style="{StaticResource MainTextBlockStyle}"
                               Margin="1" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="6">
            <WrapPanel Visibility="{Binding SelectedProtectionMeasure, Converter={StaticResource NullToVisibilityConverter}}">
                <cc:LabeledValueBox LabelText="Наименование группы мер защиты"
                                    ValueText="{Binding SelectedProtectionMeasure.NameGroup}" />

                <cc:LabeledValueBox LabelText="Наименование меры защиты"
                                    ValueText="{Binding SelectedProtectionMeasure.FullName}" />

                <cc:LabeledValueBox LabelText="Классы защищенности АСУ"
                                    ValueText="{Binding SelectedProtectionMeasure.SecurityClasses}" />
            </WrapPanel>
        </Border>
        
        <StackPanel  Grid.Row="2"
                     Grid.ColumnSpan="2"
                     Orientation="Horizontal"
                     HorizontalAlignment="Center">
            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Добавить"
                    Command="{Binding AddCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Удалить"
                    Command="{Binding DeleteCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Изменить"
                    Command="{Binding ChangeCommand}" />
        </StackPanel>
    </Grid>
</UserControl>