<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Viewer.TacticView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PragmaticAnalyzer.MVVM.Views"
             xmlns:cc="clr-namespace:PragmaticAnalyzer.CastomControls"
             xmlns:assist="clr-namespace:PragmaticAnalyzer.Assistant"
             xmlns:vmViewer="clr-namespace:PragmaticAnalyzer.MVVM.ViewModel.Viewer"
             d:DataContext="{d:DesignInstance vmViewer:TacticViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="800">
    <UserControl.Resources>
        <assist:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <Style x:Key="HeadingTextBlock"
               TargetType="TextBlock">
            <Setter Property="TextWrapping"
                    Value="Wrap" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontFamily"
                    Value="Consolas" />
        </Style>
        <Style x:Key="DynamicTextBlock"
               TargetType="TextBlock">
            <Setter Property="TextWrapping"
                    Value="Wrap" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="FontFamily"
                    Value="Consolas" />
        </Style>
        <Style x:Key="Button"
               TargetType="Button">
            <Setter Property="Width"
                    Value="100" />
            <Setter Property="Margin"
                    Value="2" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="FontSize"
                    Value="15" />
            <Setter Property="FontFamily"
                    Value="Consolas" />
        </Style>
    </UserControl.Resources>

    <Grid Grid.Row="0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Grid.Column="0"
                   Text="Условное обозначение:"
                   Style="{StaticResource MainTextBlockStyle}" />

        <TreeView Grid.Row="1"
                  Grid.Column="0"
                  ItemsSource="{Binding Tactics}"
                  SelectedItemChanged="SelectedItemChanged"
                  Style="{StaticResource RoundedTreeViewStyle}"
                  ItemContainerStyle="{StaticResource RoundedTreeViewItemStyle}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Techniques}">
                    <TextBlock Text="{Binding DisplayName}"
                               Style="{StaticResource MainTextBlockStyle}"
                               Margin="1" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <Border Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="6">
            <WrapPanel Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                <cc:LabeledValueBox LabelText="Описание"
                                    ValueText="{Binding SelectedItem.Description}" />
            </WrapPanel>
        </Border>

        <StackPanel Grid.Row="2"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Добавить"
                    Command="{Binding AddCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Удалить"
                    IsEnabled="{Binding}"
                    Command="{Binding DeleteCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Изменить"
                    IsEnabled="{Binding}"
                    Command="{Binding ChangeCommand}" />
        </StackPanel>
    </Grid>
</UserControl>