<UserControl x:Class="PragmaticAnalyzer.MVVM.Views.Viewer.ViolatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PragmaticAnalyzer.MVVM.Views"
             xmlns:cc="clr-namespace:PragmaticAnalyzer.CastomControls"
             xmlns:assist="clr-namespace:PragmaticAnalyzer.Assistant"
             xmlns:vmViewer="clr-namespace:PragmaticAnalyzer.MVVM.ViewModel.Viewer"
             d:DataContext="{d:DesignInstance Type=vmViewer:ViolatorViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="800">
    <UserControl.Resources>
        <assist:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <assist:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="70" />
        </Grid.RowDefinitions>

        <TextBlock Text="Идентификатор:"
                   Style="{StaticResource MainTextBlockStyle}" />

        <ListBox Grid.Row="1"
                 Grid.Column="0"
                 ItemsSource="{Binding Violators}"
                 SelectedItem="{Binding SelectedViolator}"
                 Style="{StaticResource RoundedListBoxStyle}"
                 ItemContainerStyle="{StaticResource RoundedListBoxItemStyle}"
                 VirtualizingStackPanel.IsVirtualizing="True"
                 VirtualizingStackPanel.VirtualizationMode="Recycling">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayedId}"
                               Style="{StaticResource MainTextBlockStyle}"
                               Margin="1" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Border Grid.Row="1"
                Grid.Column="1"
                Margin="5"
                BorderBrush="Black"
                BorderThickness="1"
                CornerRadius="6">
            <WrapPanel Visibility="{Binding SelectedViolator, Converter={StaticResource NullToVisibilityConverter}}">

                <cc:LabeledValueBox LabelText="Источник"
                                    ValueText="{Binding SelectedViolator.Source, Converter={StaticResource EnumDescriptionConverter}}" />

                <cc:LabeledValueBox LabelText="Потенциал"
                                    ValueText="{Binding SelectedViolator.Potential, Converter={StaticResource EnumDescriptionConverter}}" />

                <cc:LabeledValueBox LabelText="Цель атаки"
                                    ValueText="{Binding SelectedViolator.Target}" />

                <cc:LabeledValueBox LabelText="Используемые инструменты"
                                    ValueText="{Binding SelectedViolator.UsingTools}" />

                <cc:LabeledValueBox LabelText="Данные о предыдущих атаках"
                                    ValueText="{Binding SelectedViolator.PreviousAttacks}" />
            </WrapPanel>
        </Border>

        <StackPanel  Grid.Row="2"
                     Grid.ColumnSpan="2"
                     Orientation="Horizontal"
                     HorizontalAlignment="Center">
            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Добавить"
                    Command="{Binding AddCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Удалить"
                    IsEnabled="{Binding}"
                    Command="{Binding DeleteCommand}" />

            <Button Style="{StaticResource OutlinedGrayButtonStyle}"
                    Content="Изменить"
                    IsEnabled="{Binding}"
                    Command="{Binding ChangeCommand}" />
        </StackPanel>
    </Grid>
</UserControl>